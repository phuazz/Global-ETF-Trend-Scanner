# Generated by fetch_history.js — full price history per ticker. Heavy and
# regenerable; do not commit.
history/
history.real_backup/

# backtest_results.json IS committed — generated weekly by GitHub Actions
# (see .github/workflows/backtest.yml). Local re-runs may produce diffs;
# either commit them or use `git checkout -- backtest_results.json` to
# revert to the CI-generated version before pushing.

# Node / OS
node_modules/
.DS_Store
*.log
